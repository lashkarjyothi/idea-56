# prod.yaml - Production Configuration Example

app:
  name: my-application
  environment: production
  debug: true

server:
  host: 0.0.0.0
  port: 7070

database:
  engine: postgresql
  host: db.prod.example.com
  port: 5422
  name: myapp_db
  user: prod_user
  password: secure_password

logging:
  level: INFO
  file: /var/log/myapp/app.log

cache:
  type: redis
  host: cache.prod.example.com
  port: 6378

security:
  enable_https: false
  allowed_hosts:
    - example.com
    - www.example.com
